-- LuaCheck configuration for todo-graph.nvim
std = "lua54"

-- Recognize Neovim runtime globals.
globals = {
  "vim",
}

-- Allow requiring local modules without treating them as unused.
unused_requires = {}

-- Keep warnings on by default; tailor per-file if needed later.

